[request_definition]
r = sub, ws, obj, act

[policy_definition]
p = sub, ws, obj, act

[role_definition]
g = _, _, _

[policy_effect]
e = some(where (p.eft == allow)) && !some(where (p.eft == deny))

[matchers]
m = g(r.sub, p.sub, r.ws) && r.ws == p.ws && r.obj == p.obj && r.act == p.act